spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/airline
server:
  port: 8081